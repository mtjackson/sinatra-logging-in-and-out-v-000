<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <h1>Hello <%= Helpers.current_user[:username] %></h1>
      <h2>Balance: <%= Helpers.current_user[:balance] %></h2>

      <a href="/logout">Logout</a>

    <% else %>
      <% redirect '/error' %>
    <% end %>

  </body>
</html>
